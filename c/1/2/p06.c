#include <stdio.h>
#include <math.h>

int main()
{
  char k;
	int tot = 0, all = 0;

   while( scanf("%c" , &k ) == 1 )
	{ all++;
		if ( k == 'C' || k == 'C' )
 
	{ 
		tot++; 


}
} printf("Total Cs : %d\n" , (100*tot)/all );
}
